#include "PyEngine/Resources/AssetRegistry.hpp"

namespace PyEngine {
// Implementation is entirely inline/template in header for this singleton
// but we create the cpp file for consistency and potential future expansion
}
